{
  "name": "health-app",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "node app.js & APP_PID=$!; trap 'kill $APP_PID 2>/dev/null' EXIT; for i in {1..30}; do curl -fsS http://127.0.0.1:3000/health >/dev/null && break; sleep 1; done; curl -fsS http://127.0.0.1:3000/health && curl -fsS http://127.0.0.1:3000/status"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
